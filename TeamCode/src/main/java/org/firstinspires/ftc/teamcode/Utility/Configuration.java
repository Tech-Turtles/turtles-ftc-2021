package org.firstinspires.ftc.teamcode.Utility;

public class Configuration {

    /*
     * Opmode variables for detecting whether or not the OpMode is stuck in an infinite loop.
     * Default values are updated with the following values in RobotHardware during init.
     */
    static int msStuckInit = 5000;
    static int msStuckInitLoop = 5000;
    static int msStuckStart = 5000;
    static int msStuckLoop = 5000;
    static int msStuckStop = 1000;



}
